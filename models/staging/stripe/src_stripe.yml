version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payments
        config:
          loaded_at_field: CAST(created AS timestamp)
          freshness:
            warn_after: {count: 70000, period: hour}  
            error_after: {count: 80000, period: hour}